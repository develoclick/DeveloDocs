NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info

# Number of concurrent Chromium pages the browser pool will keep warm
BROWSER_POOL_SIZE=3
# Milliseconds before an idle browser page is recycled
BROWSER_IDLE_TIMEOUT_MS=60000
